# Done to increase the memory available to Gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod properties
mod_version = 1.1.0
maven_group = net.quest_items
archives_name = quest_items
enabled_platforms = fabric,neoforge

# Minecraft properties
minecraft_version = 1.21.1
yarn_mappings = 1.21.1+build.1

# Dependencies
fabric_loader_version = 0.16.5
fabric_api_version = 0.115.4+1.21.1
neoforge_version = 21.1.145
yarn_mappings_patch_neoforge_version = 1.21+build.4

tiny_config_version=2.3.2

# Waystones (compile-only; provided by the modpack at runtime).
# Pinned to the newest build still produced with Loom 1.7 — newer builds (21.1.36+)
# are made with Loom 1.14 which this workspace's Architectury Loom cannot consume.
# The API surface used (WaystonesAPI activate/query) is unchanged in newer versions.
waystones_version=21.1.15+1.21.1
balm_version=21.0.55+1.21.1

architectury_version=9.1.12
